// Web Component: <timer-component></timer-component>
// A timer, with start, pause, and reset buttons, a display of the time remaining,
// a progress bar that empties as the time runs out, a sound that plays when the time is up,
// 
// Should be paired with a head-up on what the next exercise is.